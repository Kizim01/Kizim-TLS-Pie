__pycache__/
*.pyc
*.pyo
.venv/

# Captures: a single scan is ~360 MB, far past GitHub's 100 MB file limit,
# and git history is permanent. Keep them on disk, never in the repo.
captures/
*.pcap
*.cloud

# KiCad local session state (regenerated by the app)
kicad/*.kicad_prl
kicad/fp-info-cache
