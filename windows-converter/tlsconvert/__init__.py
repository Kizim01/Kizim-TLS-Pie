"""
TLS-Pie desktop converter: VLP-16 captures to point clouds SketchUp can read.

The rig's geometry is NOT defined here. `rig.py` imports tls_geometry from the
scanner's own tree, so the 8.4 degree fan-zero calibration has exactly one home.
"""

__version__ = "0.1.0"
